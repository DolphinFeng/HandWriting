// 100 拆成两部分  相加和最大   10 + 0   -10  -1 + 0 

let num = 100

function getMax (num) {
    let str = num.toString()
    let maxSum 
    let left = 0, right = 1
    
    return maxSum
}

console.log(getMax(num));