import { StyleSheet } from '@mrn/react-native'

export default StyleSheet.create({
  eventItem: {
    position: 'absolute',
    borderRadius: 4,
    minHeight: 11,
    backgroundColor: '#EBF5FF',
    overflow: 'hidden',
    borderColor: '#fff'
  }
})
