<style type="text/css">
.skeleton-base-header {
    height: 52px;
    width: 100%;
    background-color: rgb(46, 46, 46);
}
.skeleton-base-header-title {
    line-height: 52px;
    margin-left: 56px;
    color: #fff;
    font-size: 16px;
    font-family: PingFangSC-Medium;
    -webkit-font-smoothing: antialiased;
}
.skeleton-base-loading-circle {
    box-sizing: border-box;
    width: 50px;
    height: 50px;
    border:5px solid #ffc300;
    border-bottom: rgba(0,0,0,0.06) 5px solid;
    border-radius: 50%;
    animation:load 1.1s infinite linear;
    margin: 0 auto;
    margin-bottom: 10px;
    -webkit-font-smoothing: antialiased;
}
@keyframes load {
    from{
        transform: rotate(0deg);
    }
    to{
        transform: rotate(360deg);
    }
}
.skeleton-base-loading {
    margin: 200px auto 0 auto;
    font-size: 14px;
    font-family: PingFangSC-Regular;
    color: rgba(0,0,0,0.84);
    text-align: center;
}
</style>