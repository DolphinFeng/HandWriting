import {OddLane} from "./OddLaneModel.js";
import {oddLayer} from "../oddLayer.js";

// export function createOddLane(positions, state, laneId, laneGroupId, laneSource, oddData) {
//     const oddLane = new OddLane(positions, state, laneId, laneGroupId, laneSource, oddData);
//     oddLayer.oddLanes.set(laneId, oddLane);
//     return oddLane;
// }
